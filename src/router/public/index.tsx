import type { RouteObject } from "react-router-dom";

/**
 * @constant RoutesPublic
 * @type {RouteObject[]}
 * @description
 * Array of public route definitions for the application.  
 * These routes can be accessed without authentication.
 *
 */
const RoutesPublic: RouteObject[] = [];

export default RoutesPublic;
